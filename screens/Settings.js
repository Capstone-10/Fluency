import React from "react";
import {
  Text,
  SafeAreaView,
  StyleSheet,
  TextInput,
  Button,
} from "react-native";

export default function VoiceAndTextTranslate() {
  return (
    <SafeAreaView>
      <Text>Testing Settings Screen With Tab Navigation</Text>
    </SafeAreaView>
  );
}
const styles = StyleSheet.create({
  input: {
    height: 40,
    margin: 12,
    borderWidth: 1,
  },
});
